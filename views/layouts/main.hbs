<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header class="header">
        <div class="container">
            <nav class="header__nav">
                <h1 class="nav__logo">
                    <a href="/">Logo</a>
                </h1>
                <ul class="nav__list">
                    <li class="nav__item">
                        <a href="/courses" class="nav__link">Courses</a>
                    </li>
                    <li class="nav__item">
                        <a href="/cart" class="nav__link">Cart</a>
                    </li>
                </ul>
                {{#if user}}
                    <a href="/account">
                        <button class="nav__btn">{{user.username.[0]}}</button>
                    </a>
                    {{else}}
                        <ul class="nav__list">
                            <a href="/login" class="nav__account">Kirish</a>
                        <a href="/signup" class="nav__account">Signup</a>
                        </ul>
                {{/if}}
            </nav>
        </div>
    </header>
    <div class="container">
        <main>
            {{{ body }}}
        </main>
    </div>
</body>
</html>